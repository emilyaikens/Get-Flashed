
export default function NewDeckForm() {
    //create new deck that belongs to this user
    //once form is created, redirect to the "manage deck" page
    //so that user can add cards to the deck
    return (
        <div>New Deck Form</div>
    )
}